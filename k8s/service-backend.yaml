apiVersion: v1
kind: Service
metadata:
  name: trivela-backend
  labels:
    app: trivela
    component: backend
spec:
  type: ClusterIP
  selector:
    app: trivela
    component: backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
